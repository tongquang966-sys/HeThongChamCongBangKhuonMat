@{
    ViewData["Title"] = "Đăng ký tài khoản";
    Layout = "_Layout";
}

<div class="container mt-5" style="max-width: 520px;">
    <h3 class="text-center fw-bold mb-4">📝 Đăng ký tài khoản nhân viên</h3>

    @if (ViewBag.Error != null)
    {
        <div class="alert alert-danger">@ViewBag.Error</div>
    }

    @if (ViewBag.Success != null)
    {
        <div class="alert alert-success">@ViewBag.Success</div>
    }

    <form method="post">
        <div class="mb-3">
            <label class="form-label">Họ tên</label>
            <input name="HoTen" class="form-control" required />
        </div>

        <div class="mb-3">
            <label class="form-label">Email (dùng để đăng nhập)</label>
            <input name="Email" type="email" class="form-control" required />
        </div>

        <div class="mb-3">
            <label class="form-label">Mật khẩu</label>
            <input name="Password" type="password" class="form-control" required />
            <small class="text-muted">
                (Đồ án: mật khẩu sẽ là <b>Mã nhân viên</b> sau khi đăng ký)
            </small>
        </div>

        <div class="d-grid">
            <button class="btn btn-primary">Đăng ký</button>
        </div>
    </form>

    <div class="text-center mt-3">
        <a href="/Account/Login">🔐 Quay lại đăng nhập</a>
    </div>
</div>
